spring.application.name=url-shortener-sb

# H2 Database Configuration for Production
spring.datasource.url=jdbc:h2:file:./data/linklytics
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=false
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# JWT Configuration
jwt.secret=${JWT_SECRET:eafb8c32ed699f0d6bed189db1695d0d56ff40b7be77bf85}
jwt.expiration=${JWT_EXPIRATION:172800000}

# Frontend URL
# frontend.url=${FRONTEND_URL:http://localhost:3000}
frontend.url=${FRONTEND_URL:https://url-shortner-frontend-abhay.netlify.app}

# Server Configuration
server.port=${PORT:8080}